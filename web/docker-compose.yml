services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ttk-web:latest
    container_name: ttk-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
