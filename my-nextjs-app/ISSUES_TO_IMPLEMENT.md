# 12 Issues προς υλοποίηση — Οδηγίες για Claude Code

> **Τι είναι αυτό το αρχείο:** προδιαγραφή. **ΔΕΝ υλοποιείται τίποτα εδώ.** Κάθε task έχει
> (α) την **ελληνική περιγραφή όπως δόθηκε** και (β) **βήματα υλοποίησης** ώστε ένας μελλοντικός
> Claude agent να ξέρει ακριβώς τι να κάνει. Οργανωμένα κατά **δυσκολία** (Simple / Medium / Hard).
> Στο **Hard** μπαίνουν μόνο όσα χρειάζονται εξωτερικό API/βιβλιοθήκη.

## Context (διάβασέ το πρώτα)

- **App:** `my-nextjs-app` — Next.js 16 (App Router), React 19, Tailwind v4, `react-leaflet` (χάρτης), όλα **δωρεάν deps**.
- **Διάβασε** το `AGENTS.md`: αυτή η έκδοση Next.js έχει breaking changes — δες `node_modules/next/dist/docs/` πριν γράψεις κώδικα.
- **Buttons:** μόνο μέσω `app/components/ui/buttonStyles.ts` (βλ. `CLAUDE.md`). Χρώματα 60/30/10 (λευκό/πράσινο green-500/πορτοκαλί orange-500).
- **Ροή σελίδων:** `/start` (αναζήτηση: προορισμός + ημερομηνίες + ταξιδιώτες) → `/plan` (πρόγραμμα/combos) · ξεχωριστές σελίδες `/activities` (λίστα δραστηριοτήτων με κάρτες) και `/map` (πραγματικός χάρτης Leaflet).
- **Activity detail modal:** `app/components/ActivityDetail/index.tsx` (ανοίγει app-wide μέσω `useApp().openActivity`).
- **Engine δεδομένων δραστηριοτήτων:** `app/components/ActivityCombinations/core/*` (activities.functions.ts, filters.data.ts, cities.data.ts, κ.λπ.).

## Καθολικές αποφάσεις (από διευκρινίσεις με τον χρήστη)

- **#2:** Κρατάμε **και** ημερομηνίες **και** διάρκεια — κύριο το πλήθος ημερών, οι ημερομηνίες προαιρετικές.
- **#4 (vibe):** **ΜΗΝ διαγράψεις** τη λογική vibe. Απλώς **κρύψ' την εντελώς** (σαν να μην υπάρχει) από **όλες** τις σελίδες (selector/φίλτρα/μπάρες). Ο υποκείμενος κώδικας (τύποι, `bestVibe`, gradients) μένει.
- **#7 & #11:** Να φαίνεται η **περιγραφή** της δραστηριότητας **και** στο trip component (πρόγραμμα) **και** στις activity κάρτες — όχι κρυμμένη πίσω από «Περισσότερα».
- **#10:** «Magic combos» = **πακέτα δραστηριοτήτων με έκπτωση** · «κάρτες έκπτωσης» = προτάσεις **city pass**.

---

# SIMPLE

## #1 — Ονόματα προορισμών εμφανίζονται στα αγγλικά

**Περιγραφή (όπως δόθηκε):**
> «στην αρχη οταν γραφω τον προορισμό, μου έρχονται μερικά στα αγγλικά (πχ barcelona)»

**Τι σημαίνει:** Στο πεδίο αναζήτησης προορισμού, κάποιες πόλεις δείχνουν αγγλικό όνομα (π.χ. "Barcelona" αντί "Βαρκελώνη").

**Αρχεία:**
- `app/start/data/destinations.ts` — η λίστα `DESTINATIONS` με `name`, `country`, `areas[].name`.
- `app/start/components/DestinationModal.tsx` — το φίλτρο (γραμμές 45–51) ψάχνει σε `d.name` + `d.country` (case-insensitive).

**Βήματα υλοποίησης:**
1. Στο `destinations.ts`, μετάτρεψε όλα τα `name` / `country` / `areas[].name` που είναι αγγλικά σε **ελληνικά** (Βαρκελώνη, Ισπανία, Παρίσι, Γαλλία, …).
2. Για να **συνεχίσει να βρίσκει** ο χρήστης γράφοντας και αγγλικά (π.χ. «barcelona»), πρόσθεσε προαιρετικό πεδίο `aliases?: string[]` σε κάθε destination (π.χ. `["barcelona","bcn"]`) και στο `DestinationModal.tsx` συμπερίλαβέ το στο φίλτρο: `d.name … || d.country … || d.aliases?.some(a => a.toLowerCase().includes(q))`.
3. Έλεγξε ότι ο `value`/label στο `StartTripSearch.tsx` (`destLabel`, γρ. 122–132) δείχνει σωστά το νέο ελληνικό όνομα.

**Σημείωση:** Καθαρά αλλαγή δεδομένων + μικρό φίλτρο. Χωρίς εξάρτηση.

---

## #6 — Μετονομασία «Υποχρεωτικές δραστηριότητες»

**Περιγραφή (όπως δόθηκε):**
> «αντί για ΥΠΟΧΡΕΩΤΙΚΕΣ ΔΡΑΣΤΗΡΙΟΤΗΤΕΣ που με αφηνει με μια απορία, αν ειμαι υποχρεωμένη να τις κάνω αυτές σωνει και ντε, μπορούμε να βάλουμε ΕΠΙΛΕΓΩ ΔΡΑΣΤΗΡΙΟΤΗΤΕΣ»

**Τι σημαίνει:** Ο τίτλος «Υποχρεωτικές δραστηριότητες» μπερδεύει. Αλλαγή σε «Επιλέγω δραστηριότητες».

**Αρχεία:**
- `app/components/ActivityCombinations/RequiredActivities.tsx` — τίτλος γρ. 47–49, υπότιτλος γρ. 50–52.

**Βήματα υλοποίησης:**
1. Άλλαξε το `<h3>` «Υποχρεωτικές δραστηριότητες» → «Επιλέγω δραστηριότητες».
2. Άλλαξε τον υπότιτλο «Το ταξίδι σου θα τις περιλαμβάνει πάντα» σε κάτι πιο ήπιο, π.χ. «Όσες διαλέξεις θα μπουν σίγουρα στο ταξίδι».
3. (Προαιρετικά) Αν το string εμφανίζεται κι αλλού (grep «Υποχρεωτικές»), ευθυγράμμισέ το.

**Σημείωση:** Μόνο κείμενο. Η λειτουργία (hard filter) μένει ίδια.

---

## #12 — Εστιατόρια/καφέ κρύβονται στο modal

**Περιγραφή (όπως δόθηκε):**
> «οταν σε μια δραστηριοτητα παταω "περισσότερα" τότε μου εμφανιζονται πραγματι κι αλλες πληροφοριες ΟΜΩΣ τα εστιατορια και το καφε δεν φαινονται. κατα τχη τα πηρα χαμπαρι οτι ειναι εκει κατω. κρυβονται»

**Τι σημαίνει:** Στο activity modal τα εστιατόρια/καφέ **υπάρχουν** αλλά «χάνονται» κάτω. Αιτία: η δεξιά στήλη έχει εσωτερικό scroll με μέγιστο ύψος, οπότε πέφτουν κάτω από το «κόψιμο».

**Αρχεία:**
- `app/components/ActivityDetail/index.tsx` — δεξιά στήλη `md:max-h-[21.75rem] md:overflow-y-auto` (γρ. 308)· ενότητα «Εστιατόρια» (γρ. 463–500).

**Βήματα υλοποίησης:**
1. Κάνε ορατά τα εστιατόρια χωρίς ψάξιμο. Επιλογές (διάλεξε μία):
   - (α) Βγάλε την ενότητα «Εστιατόρια» **έξω** από τη scrollable δεξιά στήλη, ώστε να εμφανίζεται ως δικό της block κάτω από το grid (όπως η «Περιγραφή»).
   - (β) Ή πρόσθεσε οπτικό σύρμα ότι υπάρχει κι άλλο περιεχόμενο (π.χ. fade/«scroll for more») — λιγότερο προτιμητέο.
2. Βεβαιώσου ότι όταν υπάρχει `current.restaurants.length > 0` φαίνεται **τίτλος «Εστιατόρια»** ψηλά/εμφανώς.
3. (Σχετίζεται με #11) Σκέψου να ενοποιήσεις: η ίδια ενότητα να φαίνεται και χωρίς να χρειάζεται κανένα «Περισσότερα».

**Σημείωση:** Καθαρά layout. Χωρίς εξάρτηση.

---

## #9 — (Έπαινος, όχι task)

**Περιγραφή (όπως δόθηκε):**
> «ειναι πολυ ωραιο που εχεις βαλει το ωραριο και που εχεις βαλει την κυκλικη διαδρομη»

**Action:** Καμία. Διατήρησε το ωράριο (`InfoRow "Ωράριο"` στο ActivityDetail) και την κυκλική διαδρομή (`circular` στο `ComboMap.tsx` / `DayMap.tsx`). **Μην τα χαλάσεις** στα υπόλοιπα tasks.

---

# MEDIUM

## #2 — Διάρκεια (πλήθος ημερών) + προαιρετικές ημερομηνίες

**Περιγραφή (όπως δόθηκε):**
> «αντί για ημερομηνίες ταξιδιού το καλύτερο είναι να επιλεξω ποση διάρκεια θα έχει. μόνο αυτό μας νοιάζει. και θα πρεπει να εχει την δυνατοτητα να γραψει με το χερι οσες μερες θελει. Δεν μας ενδιαφερει αν θα ειναι σαββατοκυριακο. ουτε ειναι μονο μεχρι 5 ημερες διαρκειας τα ταξιδια.»

**Απόφαση χρήστη:** Κράτα **και τα δύο** — κύριο το πλήθος ημερών (ελεύθερο, χωρίς όριο), οι ημερομηνίες **προαιρετικές**.

**Αρχεία:**
- `app/start/components/StartTripSearch.tsx` — τα date fields (γρ. 254–323), `canSearch` (γρ. 159), `handleSearch` builds query params (γρ. 172–204).
- `app/start/components/CalendarModal.tsx` — `PRESETS` (γρ. 22–29, σήμερα 1–5 + Σαββατοκύριακο).
- `app/components/ActivityCombinations/index.tsx` — `parseStartParams` (γρ. 34–61) διαβάζει `start`/`end`· `MAX_DAYS = 7` (γρ. 27) καπάκι· υπολογισμός ημερών μέσω `diffDays`.

**Βήματα υλοποίησης:**
1. Πρόσθεσε στο `StartTripSearch` ένα πεδίο **«Διάρκεια (μέρες)»**: αριθμητικό input ελεύθερης πληκτρολόγησης (state `durationDays: number | null`). Άφησε το πεδίο ημερομηνιών ως **προαιρετικό** (μην το βγάλεις).
2. Άλλαξε το `canSearch`: να επιτρέπει αναζήτηση όταν υπάρχει `dest` + (`durationDays >= 1` **ή** `range.start`). Ενημέρωσε `missingInputs` ανάλογα.
3. Στο `handleSearch`, πρόσθεσε `params.set("days", String(durationDays))` όταν δοθεί.
4. Στο `parseStartParams` (plan), διάβασε `days`: αν υπάρχει `days`, χρησιμοποίησέ το ως αριθμό ημερών· αλλιώς υπολόγισέ το από `start`/`end` (όπως τώρα).
5. **Σήκωσε/βγάλε το `MAX_DAYS = 7`** ώστε να υποστηρίζονται «όσες μέρες θέλει». Πρόσεξε side-effects: ο scheduler/η UI του προγράμματος πρέπει να αντέχει N ημέρες (έλεγξε `index.tsx`, `Calendar.tsx`, `schedule.functions.ts`). Βάλε λογικό άνω όριο ασφαλείας (π.χ. 30) για να μη σπάσει το UI/perf.
6. Στο `CalendarModal` τα presets μπορούν να μείνουν ως «παραδείγματα ημερομηνιών», αλλά μην τα κάνεις τη μόνη οδό — η διάρκεια οδηγεί.
7. Τεστ: `app/components/ActivityCombinations/core/trip.planner.test.ts` — τρέξε `npm run test:planner` μετά, και πρόσθεσε case για days > 7.

**Σημείωση:** Χωρίς εξάρτηση. Η δουλειά είναι στο plumbing του day-count + άρση του cap.

---

## #3 — Προσωπικό σημείο εκκίνησης (π.χ. ξενοδοχείο)

**Περιγραφή (όπως δόθηκε):**
> «Το σημείο εκκίνησης μπορεί να είναι κάτι άλλο... αν έβαζε το ξενοδοχείο του? υπάρχει αυτή η επιλογή? να βαλει προσωπικό σημείο.»

**Τι σημαίνει:** Σήμερα η αφετηρία της διαδρομής είναι η επιλεγμένη **περιοχή (area)** της πόλης (κέντρο ή area). Ο χρήστης θέλει να ορίζει **δικό του σημείο** (π.χ. διεύθυνση ξενοδοχείου).

**Αρχεία:**
- `app/components/ActivityCombinations/index.tsx` — `area` state (γρ. ~121) = route anchor + start marker.
- `app/components/ActivityCombinations/ComboMap.tsx` & `DayMap.tsx` — σχεδιάζουν το `start` marker «S».
- Τύποι coords: `app/components/ActivityCombinations/core/activities.functions.ts` (`Coords`).

**Βήματα υλοποίησης:**
1. Πρόσθεσε στην επιλογή αφετηρίας μια εναλλακτική «**Δικό μου σημείο**» δίπλα στις areas (UI στο plan, εκεί που επιλέγεται το `area`).
2. Δύο δυνατές μέθοδοι ορισμού σημείου (διάλεξε — **καμία δεν θέλει πληρωμένο API**):
   - (α) **Click στον χάρτη** (`DayMap.tsx` με `react-leaflet` `useMapEvents('click')`) → παίρνεις `{lat,lng}`. Μηδέν εξάρτηση.
   - (β) **Πληκτρολόγηση διεύθυνσης** → geocode με **Nominatim/OpenStreetMap** (`https://nominatim.openstreetmap.org/search?format=json&q=...`). Δωρεάν, χωρίς key (όριο 1 req/sec, βάλε debounce + προσοχή στο usage policy).
3. Κράτα το custom σημείο ως ένα `Area`-συμβατό αντικείμενο (`{ id:"custom", name:"Το ξενοδοχείο μου", coords }`) ώστε να περάσει αυτούσιο στα `ComboMap`/`DayMap` ως `start`.
4. Persist το στο state του plan (και προαιρετικά στο `trips.storage.ts` αν θες να αποθηκεύεται με το ταξίδι).

**Σημείωση:** Αν διαλέξεις (β) χρησιμοποιεί **δωρεάν** εξωτερικό geocoding (Nominatim) — όχι πληρωμένο. Η (α) δεν χρησιμοποιεί κανένα API. Γι' αυτό μένει στο Medium, όχι Hard. *(Διευκρίνισε με τον χρήστη ποια μέθοδο προτιμά πριν την υλοποίηση.)*

---

## #4 — Κρύψε τελείως το «vibe» από όλες τις σελίδες

**Περιγραφή (όπως δόθηκε):**
> «vibe: δεν χρειάζεται, δεν μας βοηθάει σε κάτι. θα νομίζει οτι θα γίνει κάτι αν επιλέξει κάτι αλλο, ειναι περιττό»

**Απόφαση χρήστη:** **ΜΗΝ διαγράψεις** τη λογική vibe — κράτα την στον κώδικα. Απλώς **κρύψε** όλα τα vibe controls σαν να μην υπάρχουν, από **κάθε** σελίδα.

**Αρχεία (σημεία που εμφανίζεται vibe ως επιλογή):**
- `app/map/components/SearchSidebar.tsx` — dropdown `label="Vibe"` (γρ. ~117–140).
- `app/map/components/VibeBar.tsx` — ολόκληρη η μπάρα vibe πάνω από τον χάρτη.
- `app/activities/components/ActivitiesExperience.tsx` — `OpenMenu "vibe"`, `toggleVibe`, και το πέρασμα `onToggleVibe` στο `FilterSidebar`.
- `app/activities/components/FilterSidebar.tsx` — ενότητα φίλτρου Vibe.
- `app/components/ActivityCombinations/core/filters.data.ts` — filter με `name: "Vibe"` (γρ. ~59–73) που εμφανίζεται στο plan `FilterSidebar`.
- **ΜΗΝ πειράξεις:** `app/activities/components/vibeStyle.tsx` (VIBE_GRADIENT/ICONS), `bestVibe`, τύπος `VibeKey` — μένουν για τα χρώματα/εικονίδια καρτών.

**Βήματα υλοποίησης:**
1. Πρόσθεσε ένα **single feature flag**, π.χ. `export const VIBE_UI_ENABLED = false;` σε ένα κοινό config (π.χ. νέο `app/config/features.ts`).
2. Σε **κάθε** σημείο επιλογής vibe παραπάνω, τύλιξε το render με `VIBE_UI_ENABLED && (...)` ή κάνε early-return, **χωρίς** να αφαιρείς τη state/τύπους. Στο `filters.data.ts`, φίλτραρε το «Vibe» φίλτρο από τη λίστα όταν `!VIBE_UI_ENABLED` (μην το σβήσεις).
3. Καθάρισε «κενά» layout: αν η αφαίρεση του control αφήνει κενή σειρά/μπάρα (π.χ. `VibeBar`), κρύψε και τον περιέκτη.
4. Βεβαιώσου ότι τα φίλτρα/ταξινομήσεις λειτουργούν **χωρίς** το vibe (default σε «όλα τα vibes»). Τα χρώματα/εικονίδια καρτών (`VIBE_GRADIENT`) **παραμένουν**.

**Σημείωση:** Στόχος = «σαν να μην υπάρχει» στο UI, με μηδενική απώλεια λογικής (εύκολο rollback αλλάζοντας το flag).

---

## #5 — Κρύψε το φίλτρο «Τουριστική προτεραιότητα / Must-see»

**Περιγραφή (όπως δόθηκε):**
> «τουριστική προτεραιοτητα: Must see. κι αυτό περιττό ειναι, μπλέξιμο πιο πολύ»

**Τι σημαίνει:** Το φίλτρο «Τουριστική προτεραιότητα» στο plan μπερδεύει. Να φύγει από το UI. (Ίδια φιλοσοφία με #4: **κρύψε το knob, κράτα το scoring** ώστε να μην αλλάξει η κατάταξη των combos.)

**Αρχεία:**
- `app/components/ActivityCombinations/core/filters.data.ts` — filter `name: "Τουριστική προτεραιότητα"` (γρ. ~118–127), `PRIORITY_WEIGHT = 3` (γρ. 51) — το βαρύτερο βάρος στο scoring.
- `app/components/ActivityCombinations/FilterSidebar.tsx` — render των φίλτρων.

**Βήματα υλοποίησης:**
1. **Κράτα** το `PRIORITY_WEIGHT` και τον υπολογισμό priority στο scoring (αλλιώς αλλάζει ποια combos βγαίνουν πρώτα).
2. Σημείωσε το συγκεκριμένο filter ως «hidden» (π.χ. πρόσθεσε `hidden?: boolean` στον τύπο filter, βάλε `hidden: true` σ' αυτό) και στο `FilterSidebar.tsx` **μην το renderάρεις** όταν `hidden`.
3. Έλεγξε ότι ο χρήστης δεν μπορεί πια να αλλάξει αυτό το βάρος από UI, αλλά το πρόγραμμα συνεχίζει να προτιμά τα must-see (default συμπεριφορά).
4. Τεστ planner (`npm run test:planner`) — επιβεβαίωσε ότι η κατάταξη combos δεν άλλαξε.

**Σημείωση:** Χωρίς εξάρτηση. Hide-only, διατήρηση engine.

---

## #7 & #11 — Περιγραφή (και κόστος ανά μέλος) ορατά στις κάρτες ΚΑΙ στο πρόγραμμα

**Περιγραφή (όπως δόθηκε):**
> #7: «ΜΕ ΤΟ ΠΟΥ ξεκινάω να επιλέω, μου εμφανίζεται το πρόγραμμα ήδη γεμάτο. Επίσης δεν ειναι ξεκάθαρο οτι υπάρχει η περιγραφή τους. Με αναγκάζει να τις επιλέξω πρώτα, και μετά να δω το κουμπι για επιπλέον πληροφορίες. Θελουμε, προκειμένου να επιλέξει δραστηριοτητα, να μπορεί να δει τι ειναι η καθε μια.»
>
> #11: «εχεις τις δραστηριοτητες μονες τους εφοσον επιλεξω προορισμο. ομως... οταν πηγαινω να την επιλεξω, στην αλλη σελιδα που μου βγαζει το προγραμμα, εχει χαθει η περιγραφη, πρεπει να πατησω "περισσότερα". Θελω να τις βλεπω ωραια όπως όταν επιλεξω τον προορισμο, να βλεπω και το κοστος ανα μελος... και να εχω την επιλογη να επιλεξω συγκεκριμενες και να μου φτιαξει το συνολικο κοστος και διαδρομη.»

**Απόφαση χρήστη:** Δείξε την **περιγραφή** της δραστηριότητας **και** στο trip component (πρόγραμμα) **και** στις activity κάρτες — όχι πίσω από «Περισσότερα». Μαζί, δείξε **κόστος ανά μέλος**.

**Αρχεία:**
- `app/components/ActivityCombinations/TripCard.tsx` / `TripPlan.tsx` — οι δραστηριότητες μέσα στο πρόγραμμα (τώρα δείχνουν λίγα· περιγραφή κρυμμένη).
- `app/activities/components/ActivityCard.tsx` — η κάρτα· σε `compact` mode το `descriptionClass = "hidden"` (γρ. 59) → η περιγραφή κρύβεται.
- `app/components/ActivityDetail/index.tsx` — έχει ήδη τον υπολογισμό κόστους ανά ηλικία (`ageRows`, γρ. 48–71) — **επαναχρησιμοποίησέ τον**.

**Βήματα υλοποίησης:**
1. **Activity κάρτες:** βεβαιώσου ότι η περιγραφή φαίνεται και στις compact κάρτες όπου χρησιμοποιούνται μέσα στο πρόγραμμα — άλλαξε το `descriptionClass` ώστε να μη γίνεται `hidden` (ή πρόσθεσε prop `showDescription`). Κράτα `line-clamp` για συμμάζεμα.
2. **Trip component:** σε `TripCard.tsx`/`TripPlan.tsx`, για κάθε δραστηριότητα της ημέρας δείξε **σύντομη περιγραφή** + **ανάλυση κόστους ανά μέλος** (όχι μόνο όνομα). Άντλησε την ανάλυση από τη logic του `ActivityDetail` (κάνε extract τη `ageRows` σε κοινό helper, π.χ. στο `activities.functions.ts`, για επαναχρήση).
3. **#7 «ήδη γεμάτο»:** κάνε ξεκάθαρο ότι ο χρήστης μπορεί να **δει & επιλέξει** δραστηριότητες πριν «κλειδώσει» το πρόγραμμα. Επιλογές: (α) δείξε περιγραφή/κόστος inline ώστε η επιλογή να γίνεται ενημερωμένα· (β) σκέψου να μην προ-γεμίζει το πρόγραμμα «σιωπηλά» — δες πώς αρχικοποιείται το πρόγραμμα στο `index.tsx` και αν θες να ξεκινά πιο «άδειο»/με σαφή πρόσκληση επιλογής. *(Αυτή η υπο-απόφαση θέλει επιβεβαίωση από τον χρήστη αν αλλάξει η αρχική κατάσταση του προγράμματος.)*
4. Σύνδεσε με #10 (κόστος ανά μέλος) και #12 (εστιατόρια ορατά) — ιδανικά γίνονται μαζί ως «ξανασχεδιασμός κάρτας δραστηριότητας».

**Σημείωση:** Χωρίς εξάρτηση. UI/UX + extract κοινής logic κόστους.

---

## #10 — Κόστος ανά μέλος + πακέτα με έκπτωση (magic combos) + city pass

**Περιγραφή (όπως δόθηκε):**
> «χαθηκε η δυνατοτητα να βλεπεις ποσο κοστίζει το κάθε άτομο για τη κάθε δραστηριοτητα. Δηλαδή έβλεπα οτι το 11 ετων κοστίζει τόσο, το 4 ετων τόσο... τωρα βλεπω ενα συνολο. χαθηκαν και τα magic combos και οι προτασεις για καρτες με εκπτωσεις»

**Απόφαση χρήστη:** «magic combos» = **πακέτα δραστηριοτήτων με έκπτωση όταν αγοράζονται μαζί** · «κάρτες έκπτωσης» = προτάσεις **city pass**.

**Αρχεία:**
- `app/components/ActivityCombinations/TripCard.tsx` — δείχνει μόνο **σύνολο** (`Συνολική τιμή: €{totalPrice}`, γρ. ~203· `totalPriceOf`, γρ. 50–55).
- `app/components/ActivityDetail/index.tsx` — η ανάλυση ανά ηλικία/οικογένεια υπάρχει αλλά πίσω από «Περισσότερα» (`ageRows`, `prices.family`).
- `app/components/ActivityCombinations/core/activities.functions.ts` — `activityPrice`/`familyKey`/`prices.family` (family bundle = ήδη μια μορφή «πακέτου με έκπτωση»).
- `app/components/ActivityCombinations/core/destinations.data.ts` — τύποι `CityPass` (γρ. 22) & `Pass` (γρ. 23)· πεδία `cityPass`/`passes` (γρ. 42–43).
- `app/components/ActivityCombinations/core/cities.data.ts` — οι πόλεις του plan (**έλεγξε αν έχουν συμπληρωμένο `cityPass`** — φαίνεται ότι λείπει data).

**Βήματα υλοποίησης:**
1. **Κόστος ανά μέλος:** δείξε στο πρόγραμμα (TripCard/TripPlan) ανάλυση τιμής ανά μέλος ανά δραστηριότητα (π.χ. «Ενήλικας €X, Παιδί 4 ετών €Y»), όχι μόνο σύνολο. Επαναχρησιμοποίησε `ageRows`/`prices` (extract κοινό helper — βλ. #7/#11 βήμα 2). Πρόσθεσε και ανά-μέλος **σύνολο ταξιδιού**.
2. **City pass (κάρτες έκπτωσης):**
   - Επιβεβαίωσε/συμπλήρωσε δεδομένα `cityPass` ανά πόλη στο `cities.data.ts`/`destinations.data.ts` (όνομα, `discountPercent`, `url`, περιγραφή).
   - Πρόσθεσε UI πρόταση στο plan: «Με την κάρτα {name} γλιτώνεις ~{discountPercent}%» + link.
3. **Magic combos (πακέτα με έκπτωση):**
   - Όρισε μοντέλο «combo discount»: σύνολο δραστηριοτήτων που μαζί έχουν μειωμένη τιμή. (Το `prices.family` καλύπτει οικογενειακό bundle ανά δραστηριότητα — για **διαδραστηριοτικά** πακέτα χρειάζεται νέα δομή δεδομένων.)
   - Πρόσθεσε δεδομένα combos ανά πόλη + υπολογισμό έκπτωσης + UI «Magic combo: αυτές οι 3 μαζί κάνουν €X αντί €Y».
   - *(Διευκρίνισε με τον χρήστη: οι εκπτώσεις πακέτων είναι πραγματικές προσφορές παρόχων ή ενδεικτικές; αυτό καθορίζει αν χρειάζονται αληθινά δεδομένα.)*

**Σημείωση:** Χωρίς εξάρτηση (όλα τοπικά δεδομένα + UI). Πολυκόμματο — σπάσ' το σε: (1) per-member cost, (2) city pass, (3) combo discounts.

---

# HARD — χρειάζεται εξωτερικό API/βιβλιοθήκη

## #8 — Τρόποι & χρόνοι μετακίνησης μεταξύ σημείων στον χάρτη

**Περιγραφή (όπως δόθηκε):**
> «στον χάρτη δεν βλέπει πια τους τρόπους μετακίνησης από το ένα σημείο στο αλλο. στην εφαρμογή μου έβγαζε μια καρτέλα κι ελεγε ολους τους δυνατους τρόπους καθώς και τον χρονο, και με τα πόδια αν ηθελες να πας»

**Τι σημαίνει:** Ανάμεσα σε δύο σημεία της διαδρομής να φαίνεται καρτέλα με όλους τους τρόπους μετακίνησης (περπάτημα/ΜΜΜ/αυτοκίνητο/ποδήλατο) + χρόνο.

**Αρχεία:**
- `app/components/ActivityCombinations/DayMap.tsx` — πραγματικός χάρτης Leaflet· **τώρα σχεδιάζει μόνο γραμμή** (`Polyline`, γρ. 98–103), χωρίς χρόνους/τρόπους.
- `app/components/ActivityCombinations/ComboMap.tsx` — αφηρημένο SVG mini-map (όχι πραγματικοί χρόνοι).
- `app/components/ActivityCombinations/core/activities.functions.ts` — υπάρχει `distanceKm` (απόσταση ευθείας).

**⚠️ ΕΞΑΡΤΗΣΗ — γι' αυτό είναι Hard. Δύο δρόμοι:**
- **(Α) Δωρεάν, χωρίς πραγματικά δεδομένα δικτύου:** υπολόγισε **εκτίμηση** χρόνου από `distanceKm` × ταχύτητα ανά τρόπο (π.χ. πεζός 5km/h, ποδήλατο 15, αστικό 20, αυτοκίνητο 30) και δείξε καρτέλα + κουμπιά deep-link σε Google Maps (`https://www.google.com/maps/dir/?api=1&origin=..&destination=..&travelmode=walking|transit|driving|bicycling`). **Μηδέν εξάρτηση/κόστος.** Τα νούμερα είναι κατά προσέγγιση.
- **(Β) Πραγματικοί χρόνοι/διαδρομές εντός app:** χρειάζεται **Directions/Routing API**:
  - **Google Directions API** — καλύτερο για **ΜΜΜ/λεωφορεία**, αλλά **πληρωμένο** (≈$5/1.000 κλήσεις μετά το δωρεάν credit) + API key + billing.
  - **Δωρεάν εναλλακτικές:** OpenRouteService (≈2.000/μέρα), Mapbox Directions (γενναιόδωρο free tier), OSRM. Καλά για περπάτημα/αυτοκίνητο/ποδήλατο· **όχι αξιόπιστα ΜΜΜ**.

**Βήματα υλοποίησης (όταν αποφασιστεί ο δρόμος):**
1. Πρόσθεσε στο `DayMap.tsx` clickable legs/markers· σε επιλογή leg, άνοιξε καρτέλα με τους τρόπους.
2. Δρόμος (Α): καθαρά τοπικός υπολογισμός + deep-links — υλοποίησε πρώτο ως MVP (χωρίς blockers).
3. Δρόμος (Β): πρόσθεσε client/fetch στο routing API (key από env var), cache αποτελεσμάτων ανά ζεύγος σημείων, fallback στο (Α) σε αποτυχία/όριο.

**❓ Πριν την υλοποίηση χρειάζεται απόφαση χρήστη** (το έχω ήδη επισημάνει): «εκτίμηση + Google Maps links (δωρεάν)» ή «πραγματικοί χρόνοι ΜΜΜ εντός app (πιθανό κόστος)». Αν επιλεγεί δωρεάν εκτίμηση, **παύει να είναι Hard** και γίνεται Medium.

---

## Σύνοψη κατά δυσκολία

| Δυσκολία | Issues |
|---|---|
| **Simple** | #1 (ελληνικά ονόματα), #6 (μετονομασία), #12 (εστιατόρια ορατά) · #9 = έπαινος |
| **Medium** | #2 (διάρκεια+ημερομηνίες), #3 (custom σημείο), #4 (κρύψε vibe), #5 (κρύψε must-see), #7&#11 (περιγραφή+κόστος inline), #10 (κόστος/μέλος + combos + city pass) |
| **Hard (εξάρτηση)** | #8 (πραγματικοί τρόποι/χρόνοι μετακίνησης → Directions API· δωρεάν εκτίμηση = υποβαθμίζεται σε Medium) |

**Εκκρεμείς αποφάσεις χρήστη πριν την υλοποίηση:** #3 (click-στον-χάρτη ή geocoding διεύθυνσης), #7 (αν αλλάξει η αρχική «γεμάτη» κατάσταση του προγράμματος), #10 (αληθινές προσφορές combos ή ενδεικτικές), #8 (δωρεάν εκτίμηση ή πληρωμένο API).
